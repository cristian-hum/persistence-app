-- TODO: save all db objects here
CREATE TABLE book(
                          ID BIGINT,
                          title VARCHAR(255),
                          author VARCHAR(255),
                          publishDate date
);

insert into book values (1, "LOTR", "JRR Tolkien", )
